# Reference-Web-App

Install:
* npm
* git
* bower
* gulp

Windows:
 * Run the "_install-dependencies.bat" file in the _build folder

Else run: 
 * bower install
 * npm install -d

Finally:
* gulp build
